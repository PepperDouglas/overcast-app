/* eslint-disable no-unused-expressions */
class DetailUnit {
    constructor(date, max, min, img){
        this.Date = date,
        this.Max = max,
        this.Min = min,
        this.Img = img
    }
}

export default DetailUnit;